# v0.4 Agent Council Migration

本文定义 think-tank `0.4` 阶段：旧 agent-council 全量迁移收口。

旧来源：

```text
/Users/condy/Desktop/ios-automation-mcp/.claude/skills/agent-council
```

## Goal

```yaml
version_track: v0.4
theme: agent_council_full_migration
status: complete
migration_style: inventory_and_abstraction
```

v0.4 的目标不是保留 `agent-council` 平行品牌，而是把它完整收编为 think-tank 的 `council mode`、runtime 原语和 Claude Code 历史适配经验。

## 完成项

```yaml
full_inventory:
  file: docs/agent-council-full-inventory.md
  status: complete

runtime_contract:
  file: docs/agent-council-runtime-migration.md
  status: complete

history_disposition:
  file: docs/agent-council-history-index.md
  status: complete

runtime_helpers:
  file: runtime/council.py
  status: verified

validation:
  file: checks/agent_council_full_migration_check.py
  status: passing
```

## 最终判断

```yaml
agent_council_migration: complete
agent_council_parallel_brand_status: removed
legacy_assets_without_disposition: 0
core_protocol_dependency_on_ios_automation_mcp: none
old_history_outputs_copied_as_truth_source: false
```

旧 agent-council 已经从独立技能降级为 think-tank 的历史实现来源。它的通用能力进入 `council mode`、`runtime/council.py`、`protocol/consensus-contract.md`、`protocol/state-result-contract.md` 和 Claude Code adapter 文档；ios-automation-mcp 私有上下文不进入 core。

