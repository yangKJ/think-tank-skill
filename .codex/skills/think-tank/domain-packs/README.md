# domain-packs

`domain-packs/` 存放可选领域包。

领域包不是 think-tank core。它们只为特定领域提供默认背景、术语、信息源、竞品表、报告模板和监控关键词。

## 设计原则

- core 保持跨领域。
- domain pack 可插拔。
- domain pack 不能改变协议。
- domain pack 可以扩展 source strategy、profile prompt 和报告模板。

## 第一批领域包

- `image-editing/`：来自旧 research agent 的图像编辑 App 研究经验。

