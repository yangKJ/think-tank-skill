name: Open Source Release

on:
  push:
    branches:
      - master
      - main
  pull_request:

jobs:
  release-gate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Run open source release suite
        run: python3 checks/open_source_release_suite.py
