# modes

`modes/` 是 think-tank 的场景模式层。

mode 负责定义不同场景下的默认角色、流程强度、输出重点和质量门禁加严项。mode 不能改变 `protocol/` 的核心协议，只能在协议范围内配置执行策略。

## 第一批 mode

- `research.md`
- `council.md`
- `review.md`
- `strategy.md`

## mode 字段

每个 mode 应说明：

- 适用场景
- 不适用场景
- 默认角色
- 流程重点
- 输出重点
- 质量门禁
- 与历史体系的关系

