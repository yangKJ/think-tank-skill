# Image2 Prompt: Research OS + Memory Runtime

Create a README infographic for think-tank 2.0 showing the "Research OS + Memory Runtime" architecture.

Visual goal: explain the full v2.0 capability layer in one polished product documentation image.

Composition:
- 16:9 horizontal architecture image
- top title: "Research OS + Memory Runtime"
- main pipeline:
  "User Intent" -> "Run Record" -> "Post-run Curation" -> "Project Memory Runtime" -> "Research OS"
- memory runtime should split into three lanes:
  - "Episodic"
  - "Semantic"
  - "Procedural"
- Research OS workspace panel includes:
  "inbox", "sources", "artifacts", "decisions", "experiments", "runbooks", "memory", "runs", "provider-ledger"
- side modules:
  "Handoff" and "Guardrails"
- bottom strip:
  "Public core defines contracts. User workspace owns project data."

Style:
- high-end technical documentation infographic
- clean bitmap PNG, not SVG-like
- crisp panels, subtle shadows, readable labels
- light theme, structured grid, restrained multi-color accents

Constraints:
- Do not expose literal private local paths.
- Do not imply public core stores private memory.
- Avoid dense tiny text.
- Aspect ratio 16:9.
