# v0.2 Runtime Hardening

本文定义 think-tank `0.2` 阶段的建设范围。

## Goal

```yaml
version_track: v0.2
theme: runtime_hardening
source_reference: old_research_think_tank_runtime
status: implemented_minimal
```

v0.2 不扩展品牌和商业化方向，只吸收旧 research think-tank 中仍有价值的 runtime 工程经验。

## Completed Contracts

```yaml
runtime_contract:
  file: protocol/runtime-contract.md
  source_reference: core/workflow.py

slot_contract:
  file: capabilities/slot-contract.md
  source_reference:
    - runtime/slot_resolver.py
    - runtime/slot_validator.py

state_result_contract:
  file: protocol/state-result-contract.md
  source_reference: core/state.py

consensus_contract:
  file: protocol/consensus-contract.md
  source_reference: core/consensus.py

research_hardening:
  file: modes/research.md
  source_reference: references/research-protocol.md
```

## Minimal Runtime Library

```yaml
runtime_planner:
  file: runtime/planner.py
  status: verified
  check: checks/runtime_planner_check.py

slot_resolver:
  file: runtime/slot_resolver.py
  status: verified
  check: checks/slot_resolver_check.py

state_result_model:
  file: runtime/state_model.py
  status: verified
  check: checks/state_model_check.py

consensus_evaluator:
  file: runtime/consensus.py
  status: verified
  check: checks/consensus_runtime_check.py
```

## Adapter Integration

```yaml
runtime_result_schema:
  file: schemas/runtime-result.schema.json
  status: verified
  check: checks/runtime_result_schema_check.py

codex_runtime_pipeline:
  file: platforms/codex/runtime/pipeline.py
  status: verified_with_local_fixture
  check: checks/codex_runtime_pipeline_check.py

claude_code_runtime_pipeline_spec:
  file: platforms/claude-code/runtime-pipeline.md
  status: specified
  check: checks/claude_runtime_pipeline_spec_check.py

runtime_e2e_fixture:
  file: examples/runtime-e2e-fixture.json
  status: verified
  check: checks/runtime_e2e_fixture_check.py
```

## Non Goals

- 不恢复旧 research 父级关系。
- 不全量搬运旧 scripts。
- 不把 Claude Code Team 写进 core protocol。
- 不把旧 `skill.yaml` 变成新配置中心。
- 不把历史 `.think-tank/` run 结果当协议真相源。

## Verification

v0.2 新增检查：

```bash
python3 checks/runtime_contract_check.py
python3 checks/slot_contract_check.py
python3 checks/consensus_contract_check.py
python3 checks/research_protocol_check.py
python3 checks/runtime_planner_check.py
python3 checks/slot_resolver_check.py
python3 checks/state_model_check.py
python3 checks/consensus_runtime_check.py
python3 checks/runtime_result_schema_check.py
python3 checks/codex_runtime_pipeline_check.py
python3 checks/claude_runtime_pipeline_spec_check.py
python3 checks/runtime_e2e_fixture_check.py
```

前四个检查证明 v0.2 contract 已写入主仓，后四个检查证明平台无关 minimal runtime library 可运行。
最后四个检查证明 Codex adapter pipeline、Claude Code adapter spec、统一 runtime schema 和 E2E fixture 已接入。

仍不证明：

- Claude Code Team 真并发。
- 完整 adapter dispatch runtime。
- 自动 result recovery。
- 外部工具链 fallback。
